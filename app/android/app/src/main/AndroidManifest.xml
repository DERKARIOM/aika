<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools" package="com.naniger.aika">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-permission android:name="android.permission.BIND_QUICK_SETTINGS_TILE" tools:targetApi="24" />

    <!-- Smart QR Code pairing: live camera scanning -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Smart QR Code pairing: local-only Wi-Fi hotspot when no Wi-Fi network is available.
         ACCESS_WIFI_STATE / CHANGE_WIFI_STATE are "normal" permissions (granted at install
         time, no runtime prompt). NEARBY_WIFI_DEVICES (Android 13+) / ACCESS_FINE_LOCATION
         (Android 12 and below) are runtime permissions required specifically by
         WifiManager.startLocalOnlyHotspot(); see https://developer.android.com/develop/connectivity/wifi/wifi-permissions -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- No `usesPermissionFlags="neverForLocation"` here on purpose: Google's own
         documentation does not clearly state that startLocalOnlyHotspot() qualifies for
         that exemption, so we don't assert something we can't verify (it would be a false
         declaration to the Play Store if wrong). We request the permission as a normal
         (potentially location-capable) grant instead. -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        tools:targetApi="33" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="32" />
    <!-- Smart QR Code pairing: auto-joining the emitter's local-only hotspot from the
         scanning device (WifiNetworkSpecifier, Android 10+). CHANGE_NETWORK_STATE is a
         "normal" permission (auto-granted, no runtime prompt); no location permission is
         required for this specific API since it connects to an already-known SSID rather
         than scanning — see https://developer.android.com/develop/connectivity/wifi/wifi-bootstrap -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Chat: local notifications for new incoming messages (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Android TV -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <!-- Camera is used for QR pairing but is not required to use the app
         (e.g. Android TV, or manual/automatic pairing instead). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:label="Aika"
        android:name="${applicationName}"
        android:usesCleartextTraffic="true"
        android:banner="@drawable/logo_aika"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
    >

        <!-- localsend: prevent multiple instances by android:launchMode="singleTask" -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.EnableCutout"
                android:value="true"
                 />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>

                <!-- enable Android TV -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>

            <!-- localsend: Receive share intent (share_handler) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

        </activity>

        <!-- Quick tile to launch the app -->
        <service
            android:name=".QuickTileService"
            android:icon="@mipmap/ic_launcher_quicktile_foreground"
            android:label="Aika"
            android:process=":quick_tile_service"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true"
            tools:targetApi="24">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>

        <!-- "Partager Aika": exposes Aika's own installed APK (see MainActivity.shareOwnApk)
             as a content:// URI so it can be handed to Android's native Sharesheet
             (Bluetooth, Nearby Share, etc.), without granting broader access than that. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
